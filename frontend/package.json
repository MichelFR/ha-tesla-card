{
  "name": "tesla-card",
  "version": "3.1.0",
  "private": true,
  "description": "Build for the Tesla Card (Home Assistant Lovelace card)",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --minify --outfile=../tesla-card/tesla-card.js",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "lit": "^3.2.0"
  }
}
